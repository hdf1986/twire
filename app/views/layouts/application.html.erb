<!DOCTYPE html>
<html>
  <head>
    <title>Twire</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="w-full min-h-full md:grid grid-rows-auto-span grid-cols-1 text-gray-600 text-sm antialiased tracking-[-.01em] py-16 lg:pb-0">
    <%= render 'layouts/navbar' %>
    <main class="grid grid-rows-auto-span">
      <div class="h-full w-full min-h-screen ">
        <div class="relative min-h-full flex flex-col">
          <div class="flex-grow w-full min-h-screen max-w:20	mx-auto xl:px-8 lg:flex">
            <div class="flex-1 min-w-0 bg-white xl:flex">
              <div class="xl:flex-shrink-0 xl:w-80 xl:border-r xl:border-gray-200 bg-white">
                <div class="pl-4 pr-6 py-6 sm:pl-6 lg:pl-8 xl:pl-0">
                  <div class="flex-1 space-y-8 py-5">
                    <div class="space-y-8 sm:justify-between sm:items-center xl:block xl:space-y-8">
                      <%= render 'layouts/profile', user: current_user %>
                    </div>
                  </div>
                </div>
              </div>
              <div class="bg-white lg:min-w-0 lg:flex-1">
                <div class="pl-4 pr-6 pt-4 pb-4 border-b border-t border-gray-200 sm:pl-6 lg:pl-8 xl:pl-6 xl:pt-6 xl:border-t-0">
                  <%= yield %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
